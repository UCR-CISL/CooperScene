# Keep the Docker build context small. Only models/, tools/ and
# requirements.txt are COPYed by the Dockerfile; everything else is excluded.
# (configs + checkpoints are pulled from Hugging Face at runtime.)
.git
work_dirs
vendor
configs
build
data
assets
docs
*.log
nohup.out
**/__pycache__
*.egg-info
**/*.so
